import { SPXCapsulesState } from './models/spx-capsules-reducer-types';


const initialState: SPXCapsulesState = {
  capsules: []
}

const spxCapsulesReducer = (state, action) => {
  switch(action.type){

    default:
      return state;
  }
}